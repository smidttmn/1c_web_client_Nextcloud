# one_c_web_client - Интеграция 1С с Nextcloud

## 📋 Описание решения

Приложение **one_c_web_client** предназначено для интеграции систем 1С с Nextcloud. Позволяет пользователям открывать базы 1С прямо в интерфейсе Nextcloud через защищенное HTTPS соединение.

## 🔧 Технические характеристики

- **Совместимость:** Nextcloud 31-32 версии
- **Протокол:** HTTPS (требуется на 1С серверах)
- **Безопасность:** CSP (Content Security Policy), HTTPS проксирование
- **Архитектура:** Прямое подключение к 1С через iframe с HTTPS

## 📁 Структура приложения

```
one_c_web_client/
├── appinfo/
│   ├── info.xml              # Метаданные приложения
│   └── routes.php            # Маршруты
├── lib/
│   ├── AppInfo/
│   │   └── Application.php   # Основной класс
│   ├── Controller/
│   │   ├── PageController.php    # Контроллер страницы
│   │   └── ConfigController.php  # Контроллер настроек
│   └── Settings/
│       ├── AdminSettings.php     # Административные настройки
│       └── AdminSection.php      # Раздел администрирования
├── templates/
│   ├── index.php             # Клиентская страница
│   └── admin_settings.php    # Настройки администратора
├── js/
│   └── index.js              # JavaScript клиентской части
└── img/
    └── app.svg               # Иконка приложения
```

## 🚀 Инструкция по установке

### 1. Подготовка сервера 1С

**Требования:**
- ✅ HTTPS на серверах 1С (обязательно!)
- ✅ Доступность серверов 1С из сети Nextcloud
- ✅ Самоподписанные сертификаты принимаются

**Проверка доступности:**
```bash
curl -kI https://10.72.1.5/sgtbuh/
# Должен вернуть HTTP/2 200
```

### 2. Установка на Nextcloud

#### Вариант А: Ручная установка

```bash
# 1. Скопируйте приложение в папку apps Nextcloud
sudo cp -r one_c_web_client /path/to/nextcloud/apps/

# 2. Установите правильные права
sudo chown -R www-data:www-data /path/to/nextcloud/apps/one_c_web_client

# 3. Установите приложение
sudo -u www-data php /path/to/nextcloud/occ app:install one_c_web_client

# 4. Включите приложение
sudo -u www-data php /path/to/nextcloud/occ app:enable one_c_web_client

# 5. Очистите кэш
sudo -u www-data php /path/to/nextcloud/occ maintenance:repair
```

#### Вариант Б: Установка через архив

```bash
# 1. Распакуйте архив с приложением
tar -xzf one_c_web_client.tar.gz -C /path/to/nextcloud/apps/

# 2. Установите права
sudo chown -R www-data:www-data /path/to/nextcloud/apps/one_c_web_client

# 3. Установите и включите приложение
sudo -u www-data php occ app:install one_c_web_client
sudo -u www-data php occ app:enable one_c_web_client
```

### 3. Настройка приложения

1. **Войдите в Nextcloud** как администратор
2. **Перейдите:** Настройки → Администрирование → **1C WebClient**
3. **Добавьте базы 1С:**
   - Название: `СГТ Бухгалтерия`
   - URL: `https://10.72.1.5/sgtbuh/`
   
   - Название: `СГТ Кадры`
   - URL: `https://10.72.1.5/zupnew/`

4. **Сохраните настройки**

### 4. Проверка работы

1. Откройте приложение через меню Nextcloud
2. Нажмите на кнопку базы 1С
3. **Примите самоподписанный сертификат** (если появится предупреждение)
4. 1С должна открыться во фрейме

## ⚙️ Настройка CSP (при необходимости)

Если 1С сервера находятся на других доменах, отредактируйте файл:

`/apps/one_c_web_client/lib/Controller/PageController.php`

Добавьте домены в метод `index()`:

```php
$csp->addAllowedFrameDomain('https://1c.example.com');
$csp->addAllowedFrameDomain('https://10.72.1.5');
$csp->addAllowedFrameDomain('https://10.72.1.6');
```

## 🔒 Требования безопасности

### Обязательные требования

1. **HTTPS на 1С серверах**
   - Без HTTPS браузеры будут блокировать подключение
   - Самоподписанные сертификаты работают

2. **Доступность сети**
   - Nextcloud должен иметь доступ к 1С серверам
   - Проверьте firewall правила

3. **CSP настройки**
   - Все домены 1С должны быть в white list
   - Настраивается в PageController.php

### Рекомендуемые настройки

1. **Доверенные сертификаты**
   - Используйте Let's Encrypt или коммерческие сертификаты
   - Избегайте предупреждений браузера

2. **Сетевая безопасность**
   - Ограничьте доступ к 1С только для Nextcloud сервера
   - Используйте firewall правила

## 🛠️ Решение проблем

### Проблема: Ошибка Mixed Content

**Причина:** 1С доступна только по HTTP

**Решение:**
1. Настройте HTTPS на 1С сервере
2. Или используйте Apache/Nginx прокси:

```apache
# Apache конфигурация
SSLProxyEngine on
ProxyPass /1c-proxy/ https://10.72.1.5/
ProxyPassReverse /1c-proxy/ https://10.72.1.5/
```

### Проблема: CSP блокирует фрейм

**Причина:** Домен 1С не в white list

**Решение:** Добавьте домен в PageController.php:
```php
$csp->addAllowedFrameDomain('https://your-1c-domain.com');
```

### Проблема: Самоподписанный сертификат

**Причина:** Браузер не доверяет сертификату

**Решение:**
1. Откройте 1С напрямую в новой вкладке
2. Примите сертификат
3. Вернитесь в Nextcloud и попробуйте снова

### Проблема: Приложение не отображается

**Причина:** Кэш Nextcloud

**Решение:**
```bash
sudo -u www-data php occ maintenance:repair
sudo -u www-data php occ app:disable one_c_web_client
sudo -u www-data php occ app:enable one_c_web_client
```

## 📊 Мониторинг и логи

### Логи Nextcloud
```bash
# Просмотр логов
sudo tail -f /path/to/nextcloud/data/nextcloud.log

# Логи Apache
sudo tail -f /var/log/apache2/error.log
```

### Проверка доступности 1С
```bash
# Проверка HTTPS
curl -kI https://10.72.1.5/sgtbuh/

# Проверка из Nextcloud
sudo -u www-data curl -kI https://10.72.1.5/sgtbuh/
```

## 📦 Обновление приложения

```bash
# 1. Отключите старую версию
sudo -u www-data php occ app:disable one_c_web_client

# 2. Замените файлы приложения
sudo rm -rf /path/to/nextcloud/apps/one_c_web_client
sudo cp -r new_one_c_web_client /path/to/nextcloud/apps/

# 3. Установите права
sudo chown -R www-data:www-data /path/to/nextcloud/apps/one_c_web_client

# 4. Включите новую версию
sudo -u www-data php occ app:enable one_c_web_client
```

## 📞 Поддержка

### Диагностика проблем

1. **Проверьте логи Nextcloud**
2. **Проверьте консоль браузера** (F12)
3. **Проверьте доступность 1С** через curl
4. **Проверьте CSP настройки**

### Часто задаваемые вопросы

**Q: Можно ли использовать HTTP вместо HTTPS?**
A: Нет, современные браузеры блокируют Mixed Content.

**Q: Работает ли с 1С Предприятие 8.3?**
A: Да, поддерживаются все версии 1С с веб-интерфейсом.

**Q: Нужен ли доступ в интернет для работы?**
A: Нет, приложение работает в локальной сети.

## 📝 История версий

### Версия 1.0.0 (Февраль 2026)
- ✅ Первая стабильная версия
- ✅ Поддержка Nextcloud 31-32
- ✅ HTTPS подключение к 1С
- ✅ CSP защита
- ✅ Адаптивный интерфейс

## 📄 Лицензия

AGPL v3 - GNU Affero General Public License

---

**Разработчик:** Nextcloud Team  
**Дата:** Февраль 2026  
**Версия:** 1.0.0
