# 🚀 БЫСТРЫЙ СТАРТ - УСТАНОВКА v10.0.1

**Текущая рабочая версия:** 10.0.1 (исправленная)  
**Дата:** 16 марта 2026

---

## 📦 СТРУКТУРА ВЕРСИЙ:

Все версии хранятся в папке `versions/`:

```
/home/smidt/nc1c/versions/
├── v10.0.0/          # Базовая (историческая)
├── v10.0.1/          # ✅ РАБОЧАЯ (рекомендуется)
└── v10.0.2/          # Резерв
```

---

## 🎯 БЫСТРАЯ УСТАНОВКА v10.0.1:

### 1. Перейдите в директорию версии:

```bash
cd /home/smidt/nc1c/versions/v10.0.1
```

### 2. Запустите установщик:

```bash
sudo ./install.sh
```

### 3. Следуйте инструкциям:

| Вопрос | Ответ |
|--------|-------|
| Продолжить установку? | **Y** |
| Добавить сервер 1С сейчас? | **Y** |
| Название базы | **БУХ СГТ** |
| URL сервера 1С | **https://10.72.1.5/sgtbuh** |
| Добавить ещё один сервер? | **N** |

---

## ✅ ПРОВЕРКА ПОСЛЕ УСТАНОВКИ:

1. Откройте: `https://cloud.smidt.keenetic.pro`
2. Нажмите `Ctrl + Shift + R`
3. Найдите иконку "1C WebClient" в меню
4. Нажмите на кнопку базы 1С

**1С должна открыться!**

---

## 🔄 ДРУГИЕ СЦЕНАРИИ:

### Приложение есть, прокси нет:

```bash
cd /home/smidt/nc1c/versions/v10.0.1
sudo ./scripts/setup_apache_proxy_auto.sh
```

### Сломался прокси:

```bash
cd /home/smidt/nc1c/versions/v10.0.1
sudo ./scripts/setup_apache_proxy_auto.sh
```

### Очистка и переустановка:

```bash
cd /home/smidt/nc1c
sudo ./reset_nextcloud.sh

cd /home/smidt/nc1c/versions/v10.0.1
sudo ./install.sh
```

---

## 📊 ЧТО ИСПРАВЛЕНО В v10.0.1:

| Проблема | v10.0.0 | v10.0.1 ✅ |
|----------|---------|-----------|
| ProxyPass без слэша | ❌ | ✅ |
| Нет проверки авторизации | ❌ | ✅ |
| Прямые ссылки на прокси | ❌ | ✅ |

---

## 📚 ПОЛНАЯ ДОКУМЕНТАЦИЯ:

```bash
# Главная по версиям
cat /home/smidt/nc1c/versions/README.md

# Документация v10.0.1
cat /home/smidt/nc1c/versions/v10.0.1/README.md
```

---

## 🆘 УСТРАНЕНИЕ ПРОБЛЕМ:

### Ошибка 404:
```bash
cd /home/smidt/nc1c/versions/v10.0.1
sudo ./scripts/setup_apache_proxy_auto.sh
```

### Ошибка 502:
```bash
curl -k https://10.72.1.5/sgtbuh/
```

### Проверка состояния:
```bash
sudo -u www-data php /var/www/nextcloud/occ app:list | grep one_c
```

---

**Версия:** 10.0.1  
**Статус:** ✅ ГОТОВО К УСТАНОВКЕ
