# one_c_web_client - Файлы готовы к загрузке! 📦

## 📍 Файлы для загрузки на NAS

**Архив:** `/home/smidt/one_c_web_client_deploy.tar.gz` (112 KB)

## 🔐 FTP Доступ

- **Сервер:** `ftp://nas_home.local/nas/Обмен/`
- **Логин:** `groot`
- **Пароль:** `211312`

## 📋 Способ 1: Через файловый менеджер (рекомендуется)

### Linux (Nautilus/Dolphin)

1. Откройте файловый менеджер
2. Нажмите `Ctrl+L` для адресной строки
3. Введите: `ftp://groot:211312@nas_home.local/nas/Обмен/`
4. Перетащите файл `/home/smidt/one_c_web_client_deploy.tar.gz`

### Windows (Проводник)

1. Откройте Проводник
2. В адресной строке: `ftp://nas_home.local/nas/Обмен/`
3. При запросе введите:
   - Логин: `groot`
   - Пароль: `211312`
4. Скопируйте файл `one_c_web_client_deploy.tar.gz` из `/home/smidt/`

## 💻 Способ 2: Через консоль с паролем

```bash
# Перейдите в директорию с архивом
cd /home/smidt

# Загрузите на FTP (введите пароль при запросе)
curl -T one_c_web_client_deploy.tar.gz \
     ftp://nas_home.local/nas/Обмен/one_c_web_client_deploy.tar.gz \
     --user groot

# Когда спросит пароль, введите: 211312
```

## 📁 Способ 3: Смонтировать FTP

```bash
# Установите curlftpfs
sudo apt install curlftpfs

# Создайте точку монтирования
sudo mkdir /mnt/nas_ftp

# Смонтируйте FTP
sudo curlftpfs groot:nas_home.local /mnt/nas_ftp -o password=211312,utf8

# Скопируйте файл
cp /home/smidt/one_c_web_client_deploy.tar.gz /mnt/nas_ftp/nas/Обмен/

# Размонтируйте
sudo umount /mnt/nas_ftp
```

## ✅ Проверка загрузки

После загрузки проверьте файл на NAS:

```bash
# Через curl
curl ftp://nas_home.local/nas/Обмен/one_c_web_client_deploy.tar.gz \
     --user groot:211312 --head
```

Должно вернуть:
```
HTTP 1.0 200 OK
Content-Type: application/x-gzip
```

## 📄 Структура на NAS

После загрузки:

```
ftp://nas_home.local/nas/Обмен/
└── one_c_web_client_deploy.tar.gz    # 112 KB
```

## 🚀 Дальнейшие действия

1. ✅ Файл загружен на NAS
2. 📥 Скачайте на сервер Nextcloud
3. 📦 Распакуйте в `/path/to/nextcloud/apps/`
4. 🔧 Установите через `occ app:install`
5. ⚙️ Настройте базы 1С

## 📞 Если проблемы

**FTP не подключается:**
- Проверьте доступность NAS: `ping nas_home.local`
- Проверьте FTP порт: `telnet nas_home.local 21`
- Используйте файловый менеджер вместо консоли

**Ошибка доступа:**
- Проверьте логин/пароль
- Убедитесь, что у пользователя `groot` есть права на запись в `/nas/Обмен/`

---

**Готово!** Файл готов к загрузке на NAS! 🎉

**Версия:** 1.0.0 | **Дата:** Февраль 2026
