# 1C WebClient - Полное руководство по установке и настройке

## 📋 О приложении

**1C WebClient** - приложение для интеграции 1С:Предприятие с Nextcloud. Позволяет открывать базы 1С прямо в интерфейсе Nextcloud через защищенное HTTPS-соединение.

### Возможности
- ✅ Настройка списка баз 1С через административную панель
- ✅ Открытие 1С во фрейме внутри Nextcloud
- ✅ Поддержка нескольких баз данных
- ✅ Безопасное HTTPS-подключение
- ✅ Совместимость с Nextcloud 31-32

### Требования
- **Nextcloud:** версии 31 или 32
- **1С:** Веб-сервер с поддержкой HTTPS (обязательно!)
- **PHP:** версия 7.4 или выше
- **Доступ:** Сетевая доступность сервера Nextcloud до серверов 1С

---

## 🚀 Пошаговая инструкция по установке

### Шаг 1: Проверка требований

Перед установкой убедитесь, что ваш сервер соответствует требованиям:

```bash
# 1. Проверьте версию Nextcloud
# Замените /path/to/nextcloud на ваш путь к Nextcloud
sudo -u www-data php /path/to/nextcloud/occ status

# Ожидаемый вывод: версия 31.x.x или 32.x.x

# 2. Проверьте версию PHP
php -v

# Ожидаемый вывод: PHP 7.4.x или выше

# 3. Проверьте доступность HTTPS на сервере 1С
# Замените URL на адрес вашей базы 1С
curl -kI https://192.168.1.100/base1c/

# Ожидаемый вывод: HTTP/2 200 OK
```

**Важно:** Если 1С доступна только по HTTP, настройте HTTPS перед установкой приложения.

---

### Шаг 2: Подготовка файлов приложения

#### Вариант А: Установка из архива (рекомендуется)

```bash
# 1. Перейдите в директорию с архивом
cd /tmp

# 2. Распакуйте архив в директорию apps Nextcloud
# Замените /path/to/nextcloud на ваш путь к Nextcloud
sudo tar -xzf one_c_web_client_deploy.tar.gz -C /path/to/nextcloud/apps/

# 3. Переименуйте директорию (если нужно)
# Некоторые архивы могут распаковаться в nc1c вместо one_c_web_client
sudo mv /path/to/nextcloud/apps/nc1c /path/to/nextcloud/apps/one_c_web_client
```

#### Вариант Б: Ручное копирование

```bash
# 1. Скопируйте директорию приложения
# Замените пути на ваши фактические пути
sudo cp -r /путь/к/исходникам/one_c_web_client /path/to/nextcloud/apps/

# 2. Убедитесь, что директория называется one_c_web_client
ls /path/to/nextcloud/apps/ | grep one_c_web_client
```

---

### Шаг 3: Установка прав доступа

**Зачем это нужно:** Nextcloud работает от имени пользователя www-data, поэтому файлы приложения должны принадлежать этому пользователю.

```bash
# 1. Установите владельца файлов
# Замените /path/to/nextcloud на ваш путь
sudo chown -R www-data:www-data /path/to/nextcloud/apps/one_c_web_client

# 2. Установите права на файлы (755 для директорий, 644 для файлов)
sudo find /path/to/nextcloud/apps/one_c_web_client -type d -exec chmod 755 {} \;
sudo find /path/to/nextcloud/apps/one_c_web_client -type f -exec chmod 644 {} \;

# 3. Проверьте права
ls -la /path/to/nextcloud/apps/one_c_web_client/
```

---

### Шаг 4: Установка приложения через OCC

**OCC** - утилита командной строки Nextcloud для управления приложением.

```bash
# 1. Перейдите в директорию Nextcloud
cd /path/to/nextcloud

# 2. Установите приложение
sudo -u www-data php occ app:install one_c_web_client

# Ожидаемый вывод:
# one_c_web_client installed

# 3. Включите приложение (если не включилось автоматически)
sudo -u www-data php occ app:enable one_c_web_client

# Ожидаемый вывод:
# one_c_web_client enabled

# 4. Очистите кэш Nextcloud
sudo -u www-data php occ maintenance:repair

# Ожидаемый вывод:
# Nextcloud is in maintenance mode, fixing ...
# Done.
```

**Возможные проблемы:**

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Command not found` | Неверный путь к occ | Проверьте путь к Nextcloud |
| `App already installed` | Приложение уже установлено | Выполните `occ app:enable` |
| `Permission denied` | Неправильные права | Выполните Шаг 3 заново |

---

### Шаг 5: Настройка баз 1С

Теперь нужно добавить адреса баз 1С в настройки приложения.

```bash
# 1. Откройте веб-интерфейс Nextcloud в браузере
# https://ваш-домен-nextcloud

# 2. Войдите под учетной записью администратора

# 3. Перейдите в настройки:
#    - Нажмите на иконку профиля (правый верхний угол)
#    - Выберите "Настройки"
#    - В левой панели выберите "Администрирование"
#    - Найдите раздел "1C WebClient"

# 4. Добавьте базу 1С:
#    - Нажмите "Добавить базу"
#    - Введите название: например, "Бухгалтерия"
#    - Введите URL: например, "https://192.168.1.100/buh/"
#    - Нажмите "Сохранить"

# 5. Повторите для других баз
```

**Примеры заполнения:**

| Поле | Значение |
|------|----------|
| Название | Бухгалтерия предприятия |
| URL | `https://192.168.1.100/buh/` |

| Поле | Значение |
|------|----------|
| Название | Зарплата и кадры |
| URL | `https://192.168.1.100/zup/` |

| Поле | Значение |
|------|----------|
| Название | Управление торговлей |
| URL | `https://192.168.1.101/ut/` |

---

### Шаг 6: Проверка работы

```bash
# 1. Откройте приложение в Nextcloud
#    - Нажмите на иконку приложений (верхняя панель)
#    - Выберите "1C WebClient"

# 2. Нажмите на кнопку с названием базы 1С

# 3. Если появляется предупреждение о сертификате:
#    - Нажмите "Дополнительно"
#    - Выберите "Перейти на сайт (небезопасно)"
#    - Это нормально для самоподписанных сертификатов

# 4. Проверьте, что 1С загрузилась во фрейме
```

**Чек-лист успешной установки:**
- [ ] Приложение отображается в меню Nextcloud
- [ ] Кнопки баз 1С активны и кликабельны
- [ ] 1С загружается во фрейме
- [ ] Нет ошибок в консоли браузера (F12)

---

## 🔧 Настройка Content Security Policy (CSP)

**Зачем это нужно:** Если сервер 1С находится на другом домене, Nextcloud может блокировать подключение из соображений безопасности.

### Когда настраивать CSP

- 1С доступна по доменному имени (например, `https://1c.example.com`)
- 1С находится на другом сервере в сети
- Появляется ошибка в консоли браузера: `Refused to display in a frame because it set 'X-Frame-Options' to 'sameorigin'`

### Как настроить CSP

```bash
# 1. Откройте файл контроллера для редактирования
# Замените /path/to/nextcloud на ваш путь
sudo nano /path/to/nextcloud/apps/one_c_web_client/lib/Controller/PageController.php

# 2. Найдите метод index() и добавьте домены 1С:

$csp->addAllowedFrameDomain('https://192.168.1.100');
$csp->addAllowedFrameDomain('https://192.168.1.101');
$csp->addAllowedFrameDomain('https://1c.example.com');

# 3. Сохраните файл (Ctrl+O, Enter) и выйдите (Ctrl+X)

# 4. Очистите кэш Nextcloud
sudo -u www-data php /path/to/nextcloud/occ maintenance:repair
```

**Пример готового фрагмента кода:**

```php
public function index(): TemplateResponse {
    $csp = new ContentSecurityPolicy();
    
    // Разрешаем загрузку фреймов с указанных доменов
    $csp->addAllowedFrameDomain('https://192.168.1.100');
    $csp->addAllowedFrameDomain('https://192.168.1.101');
    $csp->addAllowedFrameDomain('https://1c.example.com');
    
    // Разрешаем подключение скриптов
    $csp->addAllowedScriptDomain('https://192.168.1.100');
    $csp->addAllowedScriptDomain('https://192.168.1.101');
    
    $response->setContentSecurityPolicy($csp);
    return $response;
}
```

---

## 🛠️ Решение распространенных проблем

### Проблема 1: Ошибка "Mixed Content"

**Симптомы:** В консоли браузера ошибка: `Mixed Content: The page at 'https://...' was loaded over HTTPS, but requested an insecure frame 'http://...'`

**Причина:** Приложение 1С доступно только по HTTP, а Nextcloud работает по HTTPS.

**Решение:**

```bash
# Вариант 1: Настроить HTTPS на сервере 1С (рекомендуется)
# 1. Получите SSL-сертификат для сервера 1С
# 2. Настройте веб-сервер (Apache/Nginx) на поддержку HTTPS
# 3. Проверьте доступность: curl -kI https://192.168.1.100/base1c/

# Вариант 2: Использовать обратный прокси на сервере Nextcloud
# Создайте файл /etc/apache2/sites-available/1c-proxy.conf
```

**Конфигурация Apache для прокси:**

```apache
<VirtualHost *:8443>
    ServerName 1c-proxy
    
    # Включаем SSL
    SSLEngine on
    SSLCertificateFile /etc/ssl/certs/nextcloud.crt
    SSLCertificateKeyFile /etc/ssl/private/nextcloud.key
    
    # Разрешаем проксирование
    SSLProxyEngine on
    ProxyRequests off
    
    # Проксируем запросы к 1С
    ProxyPass /1c/ http://192.168.1.100/base1c/
    ProxyPassReverse /1c/ http://192.168.1.100/base1c/
    
    # Переписываем URL в ответе
    AddOutputFilterByType SUBSTITUTE text/html
    Substitute "s|http://192.168.1.100/|https://ваш-домен:8443/1c/|i"
</VirtualHost>
```

---

### Проблема 2: Бесконечная загрузка 1С

**Симптомы:** Фрейм показывает загрузку, но 1С не открывается.

**Причина:** Сервер 1С недоступен из сети Nextcloud или блокируется firewall.

**Решение:**

```bash
# 1. Проверьте доступность сервера 1С
ping 192.168.1.100

# 2. Проверьте доступность порта HTTPS
telnet 192.168.1.100 443

# 3. Проверьте из-под пользователя www-data
sudo -u www-data curl -kI https://192.168.1.100/base1c/

# 4. Проверьте правила firewall
sudo ufw status
sudo iptables -L -n

# 5. При необходимости откройте доступ
sudo ufw allow out to 192.168.1.100 port 443 proto tcp
```

---

### Проблема 3: Приложение не отображается в меню

**Симптомы:** После установки приложение не появляется в меню Nextcloud.

**Причина:** Кэш Nextcloud или приложение не включено.

**Решение:**

```bash
# 1. Проверьте статус приложения
sudo -u www-data php /path/to/nextcloud/occ app:list | grep one_c_web_client

# 2. Если приложение в списке disabled, включите его
sudo -u www-data php /path/to/nextcloud/occ app:enable one_c_web_client

# 3. Очистите кэш
sudo -u www-data php /path/to/nextcloud/occ maintenance:repair

# 4. Если не помогло, переустановите приложение
sudo -u www-data php /path/to/nextcloud/occ app:disable one_c_web_client
sudo -u www-data php /path/to/nextcloud/occ app:remove one_c_web_client
sudo -u www-data php /path/to/nextcloud/occ app:install one_c_web_client
```

---

### Проблема 4: Предупреждение о небезопасном сертификате

**Симптомы:** Браузер показывает предупреждение при открытии 1С.

**Причина:** На сервере 1С используется самоподписанный SSL-сертификат.

**Решение:**

```bash
# Вариант 1: Принять сертификат (для самоподписанных)
# 1. Откройте URL 1С в новой вкладке напрямую
# 2. Нажмите "Дополнительно" → "Перейти на сайт"
# 3. Вернитесь в Nextcloud и обновите страницу

# Вариант 2: Установить доверенный сертификат (рекомендуется для продакшена)
# 1. Получите сертификат Let's Encrypt или коммерческий сертификат
# 2. Установите его на сервер 1С
# 3. Перезапустите веб-сервер 1С
```

---

## 📊 Диагностика и логи

### Просмотр логов Nextcloud

```bash
# Логи в реальном времени
sudo tail -f /path/to/nextcloud/data/nextcloud.log

# Поиск ошибок за последние 100 строк
sudo tail -100 /path/to/nextcloud/data/nextcloud.log | grep -i error

# Логи Apache
sudo tail -f /var/log/apache2/error.log

# Логи Nginx (если используется)
sudo tail -f /var/log/nginx/error.log
```

### Диагностика через браузер

```bash
# 1. Откройте инструменты разработчика (F12)
# 2. Перейдите на вкладку "Console"
# 3. Откройте 1C WebClient
# 4. Ищите ошибки красного цвета

# Частые ошибки:
# - Mixed Content: проблема HTTP/HTTPS
# - Refused to display: проблема CSP или X-Frame-Options
# - ERR_CONNECTION_REFUSED: сервер 1С недоступен
```

### Скрипт диагностики

Создайте файл `diagnose.sh`:

```bash
#!/bin/bash

echo "=== Диагностика 1C WebClient ==="
echo ""

# Путь к Nextcloud (измените при необходимости)
NC_PATH="/path/to/nextcloud"

echo "1. Проверка статуса Nextcloud..."
sudo -u www-data php $NC_PATH/occ status

echo ""
echo "2. Проверка статуса приложения..."
sudo -u www-data php $NC_PATH/occ app:list | grep one_c_web_client

echo ""
echo "3. Проверка доступности 1С..."
read -p "Введите URL 1С (например, https://192.168.1.100/base1c/): " ONEC_URL
curl -kI "$ONEC_URL" | head -5

echo ""
echo "4. Проверка прав на файлы приложения..."
ls -la $NC_PATH/apps/one_c_web_client/ | head -10

echo ""
echo "5. Последние 5 записей в логе Nextcloud..."
sudo tail -5 $NC_PATH/data/nextcloud.log

echo ""
echo "=== Диагностика завершена ==="
```

---

## 📦 Обновление приложения

```bash
# 1. Сделайте резервную копию текущей версии
sudo cp -r /path/to/nextcloud/apps/one_c_web_client \
         /path/to/nextcloud/apps/one_c_web_client.backup

# 2. Отключите текущую версию
sudo -u www-data php /path/to/nextcloud/occ app:disable one_c_web_client

# 3. Удалите старые файлы
sudo rm -rf /path/to/nextcloud/apps/one_c_web_client

# 4. Распакуйте новую версию
sudo tar -xzf one_c_web_client_deploy.tar.gz -C /path/to/nextcloud/apps/
sudo mv /path/to/nextcloud/apps/nc1c /path/to/nextcloud/apps/one_c_web_client

# 5. Установите права
sudo chown -R www-data:www-data /path/to/nextcloud/apps/one_c_web_client

# 6. Установите новую версию
sudo -u www-data php /path/to/nextcloud/occ app:install one_c_web_client
sudo -u www-data php /path/to/nextcloud/occ app:enable one_c_web_client

# 7. Проверьте работу
sudo -u www-data php /path/to/nextcloud/occ maintenance:repair
```

---

## 🗑️ Удаление приложения

```bash
# 1. Отключите приложение
sudo -u www-data php /path/to/nextcloud/occ app:disable one_c_web_client

# 2. Удалите приложение
sudo -u www-data php /path/to/nextcloud/occ app:remove one_c_web_client

# 3. Удалите файлы
sudo rm -rf /path/to/nextcloud/apps/one_c_web_client

# 4. Очистите кэш
sudo -u www-data php /path/to/nextcloud/occ maintenance:repair
```

---

## 📞 Дополнительная помощь

### Полезные команды OCC

```bash
# Показать все установленные приложения
sudo -u www-data php occ app:list

# Показать информацию о приложении
sudo -u www-data php occ app:getpath one_c_web_client

# Проверить целостность Nextcloud
sudo -u www-data php occ integrity:check-core

# Включить режим отладки
sudo -u www-data php occ config:system:set debug --value true --type boolean
```

### Ресурсы

- **Документация Nextcloud:** https://docs.nextcloud.com/
- **Документация 1С:** https://its.1c.ru/
- **Форум поддержки:** https://forum.nextcloud.com/

---

**Версия руководства:** 2.0  
**Дата обновления:** Февраль 2026  
**Совместимость:** Nextcloud 31-32
