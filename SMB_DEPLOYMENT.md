# one_c_web_client - Готово к развертыванию! 🚀

## 📦 Файлы для копирования

Все файлы готовы в директории: `/home/smidt/nc1c/`

**Архив для установки:** `/home/smidt/one_c_web_client_deploy.tar.gz` (111 KB)

## 📋 Инструкция по копированию на SMB

### Вариант 1: Через файловый менеджер

1. Откройте `smb://smidt_gw/Sare_lan/`
2. Создайте папку `one_c_web_client`
3. Скопируйте все файлы из `/home/smidt/nc1c/`

### Вариант 2: Через консоль (если установлен smbclient)

```bash
# Смонтируйте SMB шару
sudo mount -t cifs //smidt_gw/Sare_lan /mnt/smb_share -o username=smidt_gw

# Скопируйте файлы
sudo cp -r /home/smidt/nc1c /mnt/smb_share/one_c_web_client/

# Размонтируйте
sudo umount /mnt/smb_share
```

### Вариант 3: Через архив

```bash
# Скопируйте архив на SMB
cp /home/smidt/one_c_web_client_deploy.tar.gz /путь/к/SMB/

# Распакуйте на целевом сервере
tar -xzf one_c_web_client_deploy.tar.gz -C /path/to/nextcloud/apps/
mv /path/to/nextcloud/apps/nc1c /path/to/nextcloud/apps/one_c_web_client
```

## 🚀 Установка на Nextcloud

### 1. Подготовка

```bash
# Перейдите в директорию apps Nextcloud
cd /path/to/nextcloud/apps/

# Распакуйте приложение
sudo tar -xzf /путь/к/архиву/one_c_web_client_deploy.tar.gz
sudo mv nc1c one_c_web_client
```

### 2. Установка прав

```bash
# Установите владельца
sudo chown -R www-data:www-data one_c_web_client

# Проверьте права
ls -la one_c_web_client/
```

### 3. Установка приложения

```bash
# Установите через occ
sudo -u www-data php occ app:install one_c_web_client

# Включите
sudo -u www-data php occ app:enable one_c_web_client

# Очистите кэш
sudo -u www-data php occ maintenance:repair
```

### 4. Настройка

1. Откройте Nextcloud в браузере
2. Войдите как администратор
3. Перейдите: **Настройки** → **Администрирование** → **1C WebClient**
4. Добавьте базы 1С:
   - Название: `СГТ Бухгалтерия`
   - URL: `https://10.72.1.5/sgtbuh/`
   - Название: `СГТ Кадры`
   - URL: `https://10.72.1.5/zupnew/`

## ✅ Проверка работы

1. Откройте приложение через меню Nextcloud
2. Нажмите на кнопку базы 1С
3. Примите сертификат (если появится предупреждение)
4. 1С должна открыться!

## 📄 Документация

- `README_INSTALLATION.md` - Полная инструкция по установке
- `QUICK_START.md` - Быстрый старт
- `DEPLOYMENT_STATUS.md` - Статус развертывания
- `PROJECT_HISTORY.md` - История разработки

## 🔧 Требования

- **Nextcloud:** Версия 31-32
- **PHP:** 8.0 или выше
- **1С:** HTTPS обязателен!
- **Доступ:** Nextcloud → 1С серверы

## 📞 Поддержка

При проблемах смотрите:
1. Логи Nextcloud: `/path/to/nextcloud/data/nextcloud.log`
2. Логи Apache: `/var/log/apache2/error.log`
3. Консоль браузера (F12)

---

**Готово!** Приложение готово к установке на боевой сервер! 🎉

**Версия:** 1.0.0 | **Дата:** Февраль 2026
