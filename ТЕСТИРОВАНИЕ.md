# 🧪 ТЕСТИРОВАНИЕ НА cloud.smidt.keenetic.pro

**Ветка:** `feature/proxy-with-rewrite`  
**Сервер:** 10.1.72.70 (cloud.smidt.keenetic.pro)  
**Доступ:** ssh smidt@10.1.72.70 / Apple0589

---

## 🚀 УСТАНОВКА (ИНСТРУКЦИЯ)

### Шаг 1: Подключитесь к серверу

```bash
ssh smidt@10.1.72.70
# Пароль: Apple0589
```

### Шаг 2: Скачайте скрипт установки

```bash
cd /tmp
wget https://raw.githubusercontent.com/smidttmn/one_c_web_client/feature/proxy-with-rewrite/install_test.sh
chmod +x install_test.sh
```

### Шаг 3: Запустите установку

```bash
sudo ./install_test.sh
```

### Шаг 4: Следите за выводом

Скрипт покажет:
- ✅ Каждый шаг установки
- ✅ Логи в реальном времени
- ✅ Ошибки (если есть)
- ✅ Автоматический откат при ошибке

### Шаг 5: Проверьте результат

После установки скрипт покажет:
- Путь к логу: `/tmp/one_c_install_ГГГГММДД_ЧЧММСС.log`
- URL для проверки
- Команды для диагностики

---

## 🔍 ЧТО ДЕЛАЕТ СКРИПТ

1. **Проверка прав** - подтверждение sudo
2. **Проверка Nextcloud** - поиск occ, версии
3. **Проверка Apache** - конфиг, синтаксис, модули
4. **Резервная копия** - сохранение конфига Apache
5. **Клонирование** - загрузка из GitHub (ветка feature/proxy-with-rewrite)
6. **Поиск приложения** - автоматическое нахождение
7. **Проверка файлов** - наличие required файлов
8. **Проверка ProxyController** - наличие прокси
9. **Установка** - копирование в apps/
10. **Включение** - occ app:install
11. **Очистка кэша** - maintenance:repair, memcache:clear
12. **Перезапуск Apache** - systemctl restart apache2
13. **Проверка** - app:list, логи
14. **Отчёт** - финальная информация

---

## ⚠️ АВТОМАТИЧЕСКИЙ ОТКАТ

При ошибке скрипт:
1. ❌ Отключает приложение
2. ❌ Удаляет файлы приложения
3. ❌ Восстанавливает конфиг Apache из резервной копии
4. ❌ Показывает путь к логу
5. ❌ Завершается с ошибкой

**Отправьте лог разработчику для анализа!**

---

## ✅ ПРОВЕРКА ПОСЛЕ УСТАНОВКИ

### 1. Проверьте приложение

```bash
sudo -u www-data php occ app:list | grep one_c
```

Должно быть:
```
one_c_web_client_v3: 3.2.3
```

### 2. Откройте в браузере

**Админка Nextcloud:**
```
https://cloud.smidt.keenetic.pro/index.php/settings/admin
```

**Настройки приложения:**
```
https://cloud.smidt.keenetic.pro/index.php/settings/admin/one_c_web_client_v3
```

**Клиентская часть:**
```
https://cloud.smidt.keenetic.pro/index.php/apps/one_c_web_client_v3/
```

### 3. Проверьте логи

```bash
# Логи Nextcloud
tail -f /var/www/html/nextcloud/data/nextcloud.log

# Логи Apache
tail -f /var/log/apache2/error.log

# Логи установки
cat /tmp/one_c_install_*.log
```

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка при установке

**Смотрите лог:**
```bash
cat /tmp/one_c_install_*.log | tail -100
```

**Отправьте разработчику:**
```bash
# Скопируйте лог
cp /tmp/one_c_install_*.log /tmp/
```

### Приложение не работает

**Проверьте наличие файлов:**
```bash
ls -la /var/www/html/nextcloud/apps/one_c_web_client_v3/
```

**Проверьте права:**
```bash
chown -R www-data:www-data /var/www/html/nextcloud/apps/one_c_web_client_v3
chmod -R 755 /var/www/html/nextcloud/apps/one_c_web_client_v3
```

**Очистите кэш:**
```bash
sudo -u www-data php occ maintenance:repair
sudo -u www-data php occ memcache:clear
systemctl restart php8.1-fpm
systemctl restart apache2
```

### Ошибка 500

**Проверьте логи:**
```bash
sudo -u www-data php occ log:read | tail -50
```

**Попробуйте переустановить:**
```bash
# Удалите приложение
sudo -u www-data php occ app:remove one_c_web_client_v3
rm -rf /var/www/html/nextcloud/apps/one_c_web_client_v3

# Очистите кэш
sudo -u www-data php occ maintenance:repair

# Установите заново
cd /tmp
git clone -b feature/proxy-with-rewrite https://github.com/smidttmn/one_c_web_client.git
cd one_c_web_client
sudo ./install_test.sh
```

---

## 📊 ДИАГНОСТИКА

### Полная информация о системе

```bash
echo "=== Сервер ==="
hostname
cat /etc/os-release | grep PRETTY_NAME

echo "=== Nextcloud ==="
sudo -u www-data php occ status

echo "=== PHP ==="
php -v

echo "=== Apache ==="
apache2 -v
a2query -m proxy proxy_http headers rewrite ssl

echo "=== Приложения ==="
sudo -u www-data php occ app:list --enabled

echo "=== Логи ==="
sudo -u www-data php occ log:read | tail -50
```

---

## 📞 ОТПРАВКА ОТЧЁТА

**Если что-то не работает, отправьте:**

1. **Лог установки:**
   ```bash
   cat /tmp/one_c_install_*.log > /tmp/install_log.txt
   ```

2. **Лог Nextcloud:**
   ```bash
   sudo -u www-data php occ log:read > /tmp/nc_log.txt
   ```

3. **Информацию о приложении:**
   ```bash
   sudo -u www-data php occ app:list | grep one_c > /tmp/app_info.txt
   ```

4. **Версию PHP и Apache:**
   ```bash
   php -v && apache2 -v > /tmp/server_info.txt
   ```

**Отправьте все 4 файла разработчику!**

---

## 🎯 ЦЕЛЬ ТЕСТИРОВАНИЯ

1. ✅ Установить приложение без ошибок
2. ✅ Проверить работу прокси
3. ✅ Проверить настройку баз 1С
4. ✅ Проверить работу клиентской части
5. ✅ Исправить все найденные ошибки
6. ✅ Создать идеальную версию для GitHub

---

## 📋 ССЫЛКИ

**Репозиторий:**
```
https://github.com/smidttmn/one_c_web_client
```

**Ветка с приложением:**
```
https://github.com/smidttmn/one_c_web_client/tree/feature/proxy-with-rewrite
```

**Задача в bd:**
```
nc1c-8ou - Тестирование и отладка установки
```

---

**Готово к тестированию!** 🎉
