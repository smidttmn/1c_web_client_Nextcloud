# 📦 ФАЙЛЫ ДЛЯ УСТАНОВКИ НА ПРОДАКШЕН

## 📍 Где взять файлы

### Вариант 1: С этого сервера (cloud.smidt.keenetic.pro)

Файлы готовы в директории:
```
/home/smidt/for_danil_deploy/
├── one_c_web_client_v3_deploy.tar.gz    (10 KB)
└── INSTRUCTION.md                        (инструкция)
```

**Как забрать:**
```bash
# По SSH с продакшена (10.72.1.111):
scp root@cloud.smidt.keenetic.pro:/home/smidt/for_danil_deploy/one_c_web_client_v3_deploy.tar.gz /tmp/
```

---

### Вариант 2: По сети Windows

Путь: `\\10.72.1.171\L$\Drive_NC_1c\v3\`

**Примечание:** У меня не получилось записать в эту папку - нужен правильный пароль.

---

## 🚀 КРАТКАЯ ИНСТРУКЦИЯ ПО УСТАНОВКЕ

### 1. Распаковать архив на продакшене

```bash
cd /tmp
tar -xzf one_c_web_client_v3_deploy.tar.gz
```

### 2. Скопировать в Nextcloud

```bash
cp -r one_c_web_client_v3 /var/www/html/nextcloud/apps/
chown -R www-data:www-data /var/www/html/nextcloud/apps/one_c_web_client_v3
```

### 3. Установить приложение

```bash
sudo -u www-data php /var/www/html/nextcloud/occ app:install one_c_web_client_v3
```

### 4. Включить модули Apache

```bash
a2enmod proxy proxy_http rewrite headers ssl_proxy
```

### 5. Настроить Apache

**Добавить в `/etc/apache2/sites-available/nextcloud.conf`** (ДО директории Directory!):

```apache
# === ИСКЛЮЧЕНИЯ (ОБЯЗАТЕЛЬНО ДО ProxyPassMatch!) ===
ProxyPass /core !
ProxyPass /apps !
ProxyPass /dist !
ProxyPass /js !
ProxyPass /css !
ProxyPass /l10n !
ProxyPass /index.php !

# === ПРОВЕРКА АВТОРИЗАЦИИ ===
<Location ~ "^/([a-zA-Z0-9_-]+)/">
    RewriteEngine On
    RewriteCond %{HTTP_COOKIE} !nc_username [NC]
    RewriteRule ^.*$ /index.php/login?redirect_url=%{REQUEST_URI} [R=302,L]
</Location>

# === ПРОКСИ ДЛЯ 1С ===
ProxyPassMatch ^/([a-zA-Z0-9_-]+)/(.*)$ https://10.72.1.5/$1/$2 retry=0 timeout=60
ProxyPassReverse ^/([a-zA-Z0-9_-]+)/(.*)$ https://10.72.1.5/$1/$2

# === CSP ===
<IfModule mod_headers.c>
    Header always set Content-Security-Policy "frame-src *; script-src * 'unsafe-inline' 'unsafe-eval';"
</IfModule>
```

### 6. Перезагрузить Apache

```bash
apache2ctl configtest
systemctl restart apache2
```

### 7. Очистить кэш Nextcloud

```bash
sudo -u www-data php /var/www/html/nextcloud/occ maintenance:repair
```

---

## 🔧 ИСПРАВЛЕНИЕ ОШИБОК

### Ошибка files_trackdownloads (НЕ НАША!)

```bash
sudo -u www-data php /var/www/html/nextcloud/occ app:disable files_trackdownloads
```

### Ошибка 404 для JS/CSS

```bash
sudo -u www-data php /var/www/html/nextcloud/occ maintenance:repair
```

Очистить кэш браузера: **Ctrl+Shift+R**

### Ошибка 500 при открытии onec

Проверить логи:
```bash
tail -100 /var/www/html/nextcloud/data/nextcloud.log
journalctl -u apache2 -n 50 --no-pager
```

---

## ✅ ПРОВЕРКА

```bash
# Статус приложения
sudo -u www-data php /var/www/html/nextcloud/occ app:list | grep one_c

# Должно показать: one_c_web_client_v3: 3.0.2
```

**В браузере:**
1. Открыть Nextcloud
2. Нажать "1С:Предприятие" в меню
3. Настроить базы в админке
4. Открыть 1С

---

## 📞 ЕСЛИ ЧТО-ТО НЕ ТАК

1. Проверить что файлы на месте:
   ```bash
   ls -la /var/www/html/nextcloud/apps/one_c_web_client_v3/js/
   ```

2. Проверить конфиг Apache:
   ```bash
   grep -A 5 "ProxyPass" /etc/apache2/sites-available/nextcloud.conf
   ```

3. Проверить логи:
   ```bash
   tail -f /var/log/apache2/error.log
   ```

---

**Версия:** 3.0.2  
**Дата:** 10 марта 2026  
**Создано:** /home/smidt/for_danil_deploy/
