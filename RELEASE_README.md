# nc1c - Интеграция 1С с Nextcloud

**Версия:** 1.0.0  
**Совместимость:** Nextcloud 31-32  
**Статус:** ✅ Рабочая версия

---

## 📋 Описание

Приложение для интеграции 1С:Предприятие с Nextcloud. Позволяет открывать базы 1С прямо в интерфейсе Nextcloud через защищенное HTTPS соединение.

### Возможности

- 🔐 Подключение к 1С через HTTPS
- 🎯 Управление базами через админ-панель
- 📱 Адаптивный интерфейс
- 🔒 Защита CSP (Content Security Policy)
- 🌐 Работа из локальной и внешней сети

---

## 📦 Установка

### Вариант 1: Автоматическая установка (рекомендуется)

**Шаг 1.** Запустите скрипт установки от имени root:

```bash
# Перейдите в директорию с приложением
cd /путь/к/nc1c

# Запустите установку
sudo bash install.sh
```

**Что делает скрипт:**
- Проверяет структуру приложения
- Копирует файлы в apps Nextcloud
- Устанавливает правильные права (www-data)
- Устанавливает приложение через OCC
- Очищает кэш Nextcloud

---

### Вариант 2: Ручная установка

**Шаг 1.** Скопируйте приложение в директорию apps Nextcloud:

```bash
# Замените /путь/к/nextcloud на ваш путь к Nextcloud
sudo cp -r nc1c /путь/к/nextcloud/apps/
```

> **Примечание:** Стандартные пути к Nextcloud:
> - `/var/www/nextcloud/` (Ubuntu/Debian)
> - `/var/www/html/nextcloud/` (CentOS)
> - `/srv/www/nextcloud/` (openSUSE)

**Шаг 2.** Установите правильные права:

```bash
# Замените /путь/к/nextcloud на ваш путь
sudo chown -R www-data:www-data /путь/к/nextcloud/apps/nc1c
```

> **Важно:** В разных дистрибутивах пользователь может называться по-разному:
> - `www-data` (Ubuntu/Debian)
> - `apache` (CentOS/RHEL)
> - `wwwrun` (openSUSE)

**Шаг 3.** Установите приложение через OCC:

```bash
# Замените /путь/к/nextcloud на ваш путь
sudo -u www-data php /путь/к/nextcloud/occ app:install nc1c
```

**Шаг 4.** Включите приложение:

```bash
sudo -u www-data php /путь/к/nextcloud/occ app:enable nc1c
```

**Шаг 5.** Очистите кэш:

```bash
sudo -u www-data php /путь/к/nextcloud/occ maintenance:repair
```

---

## ⚙️ Настройка

**Шаг 1.** Откройте Nextcloud в браузере

**Шаг 2.** Войдите под учетной записью администратора

**Шаг 3.** Перейдите в настройки администрирования:
- Нажмите на аватар (справа сверху)
- Выберите **"Настройки"**
- В левом меню выберите **"Администрирование"** → **"1C WebClient"**

**Шаг 4.** Добавьте базы 1С:

Нажмите **"Добавить базу"** и заполните:

| Поле | Пример | Описание |
|------|--------|----------|
| **Название** | `Бухгалтерия` | Отображаемое имя базы |
| **URL** | `https://192.168.1.100/1c/` | HTTPS адрес веб-клиента 1С |

> **Важно:** URL должен начинаться с `https://`!
> 
> Примеры правильных URL:
> - `https://192.168.1.100/1c/` (локальная сеть)
> - `https://1c.example.com/` (доменное имя)
> - `https://10.0.0.50/sgtbuh/` (IP адрес)

**Шаг 5.** Нажмите **"Сохранить настройки"**

---

## 👥 Использование

**Шаг 1.** Откройте приложение через меню Nextcloud:
- Нажмите на иконку приложений (справа сверху)
- Найдите и выберите **"1C WebClient"**

**Шаг 2.** Выберите нужную базу из списка кнопок

**Шаг 3.** При первом подключении браузер может предупредить о самоподписанном сертификате:
- Нажмите **"Дополнительно"**
- Выберите **"Перейти на сайт (небезопасно)"**
- 1С откроется во фрейме Nextcloud

---

## 📋 Требования

| Компонент | Требование | Примечание |
|-----------|------------|------------|
| **Nextcloud** | 31-32 версии | Проверьте в Настройки → О системе |
| **PHP** | 8.0 или выше | `php -v` |
| **1С** | HTTPS обязателен! | HTTP блокируется браузерами |
| **Сеть** | Доступ к 1С серверам | Проверьте ping/firewall |

---

## 🔧 Решение проблем

### Ошибка: "Mixed Content"

**Симптом:** Браузер блокирует подключение к 1С

**Причина:** 1С доступна только по HTTP, а Nextcloud работает по HTTPS

**Решение:**
1. Настройте HTTPS на сервере 1С
2. Или используйте самоподписанный сертификат (примите в браузере)

---

### Ошибка: "Content Security Policy блокирует фрейм"

**Симптом:** Ошибка CSP в консоли браузера

**Причина:** Домен 1С не добавлен в разрешенные

**Решение:** Отредактируйте файл `lib/Controller/PageController.php`:

```php
// Найдите метод index() и добавьте домен:
$csp->addAllowedFrameDomain('https://192.168.1.100');
$csp->addAllowedFrameDomain('https://1c.example.com');
```

---

### Ошибка: "Сертификат не доверяется"

**Симптом:** Браузер предупреждает о небезопасном соединении

**Причина:** Самоподписанный сертификат на сервере 1С

**Решение:**
1. Откройте URL 1С напрямую в новой вкладке
2. Примите сертификат (Дополнительно → Перейти на сайт)
3. Вернитесь в Nextcloud и попробуйте снова

---

### Приложение не отображается в меню

**Симптом:** Нет кнопки "1C WebClient" в меню приложений

**Решение:**
```bash
# Очистите кэш Nextcloud
sudo -u www-data php /путь/к/nextcloud/occ maintenance:repair

# Переустановите приложение
sudo -u www-data php /путь/к/nextcloud/occ app:disable nc1c
sudo -u www-data php /путь/к/nextcloud/occ app:enable nc1c
```

---

### Ошибка при установке через OCC

**Симптом:** `Command not found` или `Could not open input file`

**Причина:** Неверный путь к OCC или Nextcloud

**Решение:**
```bash
# Найдите где установлен Nextcloud
sudo find /var/www -name "occ" -type f

# Используйте найденный путь
sudo -u www-data php /найденный/путь/occ app:install nc1c
```

---

## 📁 Структура приложения

```
nc1c/
├── appinfo/              # Конфигурация приложения
│   ├── info.xml          # Метаданные (версия, совместимость)
│   └── routes.php        # Маршруты URL
├── lib/                  # PHP код (логика приложения)
│   ├── AppInfo/          # Информация о приложении
│   ├── Controller/       # Обработчики запросов
│   └── Settings/         # Настройки администрирования
├── templates/            # HTML шаблоны страниц
│   ├── index.php         # Клиентская страница
│   └── admin_settings.php # Настройки администратора
├── js/                   # JavaScript код
│   └── index.js          # Логика клиентской части
├── img/                  # Изображения (иконки)
├── l10n/                 # Переводы (локализация)
│   └── ru.json           # Русский язык
├── README.md             # Этот файл
├── LICENSE               # Лицензия AGPL v3
└── install.sh            # Скрипт автоматической установки
```

---

## 📄 Лицензия

AGPL v3 - GNU Affero General Public License

Приложение распространяется бесплатно. Исходный код доступен для модификации.

---

## 📞 Поддержка

### Логи для диагностики

При возникновении проблем проверьте логи:

```bash
# Лог Nextcloud
sudo tail -f /путь/к/nextcloud/data/nextcloud.log

# Лог Apache
sudo tail -f /var/log/apache2/error.log

# Лог PHP-FPM
sudo tail -f /var/log/php*-fpm.log
```

### Консоль браузера

Нажмите **F12** в браузере и откройте вкладку **"Console"** для просмотра ошибок JavaScript.

---

## 📊 Статистика

| Параметр | Значение |
|----------|----------|
| Версия | 1.0.0 |
| Файлов | 50+ |
| Размер | ~100 KB |
| Дата релиза | Февраль 2026 |
| Совместимость | Nextcloud 31-32 |
| Лицензия | AGPL v3 |

---

**Готово к использованию!** ✅

**Версия:** 1.0.0 | **Дата:** Февраль 2026
