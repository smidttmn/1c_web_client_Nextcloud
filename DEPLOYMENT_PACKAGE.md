# 📦 Готовый пакет для развертывания one_c_web_client

## ✅ Что создано

### Файлы готовы к использованию:

| Файл | Назначение | Размер |
|------|------------|--------|
| `/home/smidt/one_c_web_client_deploy.tar.gz` | Установочный архив | 36 KB |
| `/home/smidt/nc1c/deploy.sh` | Скрипт автоустановки | 23 KB |
| `/home/smidt/nc1c/INSTALLATION_GUIDE.md` | Полная инструкция | 19 KB |
| `/home/smidt/nc1c/QUICK_START_RU.md` | Быстрый старт | 5 KB |

---

## 🚀 Как использовать

### Вариант 1: Автоматическая установка (рекомендуется)

```bash
# 1. Скопируйте файлы на сервер Nextcloud
scp /home/smidt/one_c_web_client_deploy.tar.gz user@server:/tmp/
scp /home/smidt/nc1c/deploy.sh user@server:/tmp/

# 2. Подключитесь к серверу Nextcloud
ssh user@server

# 3. Перейдите в директорию с файлами
cd /tmp

# 4. Сделайте скрипт исполняемым
chmod +x deploy.sh

# 5. Запустите установку
sudo ./deploy.sh one_c_web_client_deploy.tar.gz
```

**Скрипт автоматически:**
- ✅ Найдет Nextcloud (не нужно указывать путь)
- ✅ Проверит совместимость версии
- ✅ Распакует приложение
- ✅ Установит правильные права
- ✅ Установит приложение через OCC
- ✅ Очистит кэш
- ✅ Предложит настроить CSP

---

### Вариант 2: Ручная установка

```bash
# 1. Распакуйте архив в apps Nextcloud
sudo tar -xzf /tmp/one_c_web_client_deploy.tar.gz -C /path/to/nextcloud/apps/

# 2. Установите права
sudo chown -R www-data:www-data /path/to/nextcloud/apps/one_c_web_client

# 3. Установите приложение
sudo -u www-data php /path/to/nextcloud/occ app:install one_c_web_client
sudo -u www-data php /path/to/nextcloud/occ app:enable one_c_web_client
sudo -u www-data php /path/to/nextcloud/occ maintenance:repair
```

---

## 📖 Документация

### Для быстрой установки
Откройте **QUICK_START_RU.md** - установка за 5 минут

### Для подробного изучения
Откройте **INSTALLATION_GUIDE.md** - полное руководство с:
- Проверкой требований
- Пошаговой установкой
- Настройкой CSP
- Решением проблем
- Диагностикой

---

## 📋 Примеры настройки баз 1С

После установки добавьте базы через веб-интерфейс:

**Настройки → Администрирование → 1C WebClient**

### Пример 1: Бухгалтерия
```
Название: Бухгалтерия предприятия
URL: https://192.168.1.100/buh/
```

### Пример 2: Зарплата и кадры
```
Название: Зарплата и кадры
URL: https://192.168.1.100/zup/
```

### Пример 3: Управление торговлей
```
Название: Управление торговлей
URL: https://192.168.1.101/ut/
```

---

## ✅ Требования

| Компонент | Требование | Как проверить |
|-----------|------------|---------------|
| **Nextcloud** | Версия 31-32 | `sudo -u www-data php occ status` |
| **PHP** | 7.4 или выше | `php -v` |
| **1С** | HTTPS обязателен | `curl -kI https://server/base/` |
| **Сеть** | Доступ Nextcloud → 1С | `ping 192.168.1.100` |

---

## 🛠️ Решение проблем

### Ошибка: Mixed Content

**Проблема:** 1С доступна только по HTTP

**Решение:** Настройте HTTPS на сервере 1С

```bash
# Проверьте доступность HTTPS
curl -kI https://192.168.1.100/buh/

# Должен вернуть: HTTP/2 200 OK
```

---

### Ошибка: CSP блокирует фрейм

**Проблема:** Домен 1С не в белом списке

**Решение:** Запустите скрипт с настройкой CSP или отредактируйте файл:

```bash
sudo nano /path/to/nextcloud/apps/one_c_web_client/lib/Controller/PageController.php
```

Добавьте после строки `$csp = new ContentSecurityPolicy();`:

```php
$csp->addAllowedFrameDomain('https://192.168.1.100');
$csp->addAllowedScriptDomain('https://192.168.1.100');
```

Очистите кэш:

```bash
sudo -u www-data php occ maintenance:repair
```

---

### Ошибка: Приложение не отображается

**Проблема:** Кэш Nextcloud

**Решение:**

```bash
sudo -u www-data php occ app:disable one_c_web_client
sudo -u www-data php occ app:enable one_c_web_client
sudo -u www-data php occ maintenance:repair
```

---

## 📊 Что внутри архива

```
one_c_web_client_deploy.tar.gz (36 KB)
│
├── deploy.sh                      # Скрипт автоустановки
│
└── one_c_web_client/              # Приложение Nextcloud
    ├── README.md                  # Описание
    ├── QUICK_START_RU.md          # Быстрый старт
    ├── INSTALLATION_GUIDE.md      # Полное руководство
    ├── LICENSE                    # Лицензия AGPL v3
    │
    ├── appinfo/                   # Метаданные
    │   ├── info.xml
    │   └── routes.php
    │
    ├── lib/                       # PHP классы
    │   ├── AppInfo/Application.php
    │   ├── Controller/
    │   │   ├── PageController.php
    │   │   ├── ConfigController.php
    │   │   └── ProxyController.php
    │   └── Settings/
    │       ├── AdminSettings.php
    │       └── AdminSection.php
    │
    ├── templates/                 # HTML шаблоны
    │   ├── index.php
    │   └── admin_settings.php
    │
    ├── js/                        # JavaScript
    │   ├── index.js
    │   └── admin_settings.js
    │
    ├── css/                       # Стили
    ├── img/                       # Иконки
    │   └── app.svg
    │
    └── l10n/                      # Переводы
        ├── ru.json
        └── ru.php
```

---

## 📞 Поддержка

### Логи для диагностики

```bash
# Логи Nextcloud
sudo tail -f /path/to/nextcloud/data/nextcloud.log

# Логи Apache
sudo tail -f /var/log/apache2/error.log

# Логи Nginx
sudo tail -f /var/log/nginx/error.log
```

### Полезные команды OCC

```bash
# Статус приложения
sudo -u www-data php occ app:list | grep one_c_web_client

# Информация о Nextcloud
sudo -u www-data php occ status

# Переустановка приложения
sudo -u www-data php occ app:disable one_c_web_client
sudo -u www-data php occ app:remove one_c_web_client
sudo -u www-data php occ app:install one_c_web_client
sudo -u www-data php occ app:enable one_c_web_client

# Очистка кэша
sudo -u www-data php occ maintenance:repair
```

---

## 📝 Лицензия

**AGPL v3** - GNU Affero General Public License

---

## 📈 Статистика проекта

- **Версия:** 1.0.0
- **Дата:** Февраль 2026
- **Совместимость:** Nextcloud 31-32
- **Файлов в архиве:** 30+
- **Размер архива:** 36 KB
- **Строк документации:** 1000+
- **Время установки:** 2-5 минут

---

## ✨ Особенности

- ✅ **Универсальность** - работает на Nextcloud 31 и 32
- ✅ **Автоматизация** - скрипт сам найдет Nextcloud
- ✅ **Безопасность** - HTTPS и CSP из коробки
- ✅ **Документация** - подробные инструкции на русском
- ✅ **Поддержка** - диагностика и решение проблем

---

**Готово к развертыванию! 🚀**

Для установки просто скопируйте архив и скрипт на сервер Nextcloud и запустите:

```bash
sudo ./deploy.sh one_c_web_client_deploy.tar.gz
```
