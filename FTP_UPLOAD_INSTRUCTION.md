# Инструкция по копированию на FTP: ftp://nas_home.local/nas/Обмен/

## 📁 Файлы для копирования

**Архив:** `one_c_web_client_deploy.tar.gz` (112 KB)
**Репозиторий:** папка `nc1c/`

## 🔐 Вариант 1: Через консоль (рекомендуется)

```bash
# Установите lftp (если нет)
sudo apt install lftp

# Подключитесь к FTP
lftp ftp://nas_home.local/nas/Обмен/

# Введите пароль при запросе
# Введите: lcd /home/smidt
# Введите: put one_c_web_client_deploy.tar.gz
# Введите: bye
```

## 🖥️ Вариант 2: Через файловый менеджер

### Linux (Nautilus/Dolphin)

1. Откройте файловый менеджер
2. Нажмите `Ctrl+L` (адресная строка)
3. Введите: `ftp://nas_home.local/nas/Обмен/`
4. Введите логин: `smidt_gw`
5. Введите пароль
6. Скопируйте файл `one_c_web_client_deploy.tar.gz`

### Windows (Проводник)

1. Откройте Проводник
2. В адресной строке: `ftp://nas_home.local/nas/Обмен/`
3. Введите логин: `smidt_gw`
4. Введите пароль
5. Скопируйте файл `one_c_web_client_deploy.tar.gz`

## 📦 Вариант 3: Через curl

```bash
# Копирование архива
curl -T /home/smidt/one_c_web_client_deploy.tar.gz \
     ftp://nas_home.local/nas/Обмен/one_c_web_client_deploy.tar.gz \
     --user smidt_gw

# Вас попросят ввести пароль
```

## 📂 Вариант 4: Через smbclient

```bash
# Установите smbclient (если нет)
sudo apt install smbclient

# Подключитесь к SMB
smbclient //nas_home.local/nas -U smidt_gw

# Введите пароль
# Введите: cd Обмен
# Введите: lcd /home/smidt
# Введите: put one_c_web_client_deploy.tar.gz
# Введите: bye
```

## ✅ Проверка

После копирования проверьте, что файлы доступны:

```bash
# Проверка через curl
curl ftp://nas_home.local/nas/Обмен/one_c_web_client_deploy.tar.gz \
     --user smidt_gw --head
```

## 📄 Структура на FTP

После копирования структура должна быть:

```
ftp://nas_home.local/nas/Обмен/
├── one_c_web_client_deploy.tar.gz    # Архив для установки
└── nc1c/                              # Исходный код
    ├── appinfo/
    ├── lib/
    ├── templates/
    ├── js/
    ├── img/
    ├── README_INSTALLATION.md
    ├── QUICK_START.md
    └── ...
```

## 🔑 Примечания

- **Логин:** `smidt_gw`
- **Пароль:** (ваш пароль от nas_home.local)
- **Путь:** `/nas/Обмен/`
- **Протокол:** FTP / SMB

---

**Готово!** Файлы скопированы на FTP сервер! 🎉
