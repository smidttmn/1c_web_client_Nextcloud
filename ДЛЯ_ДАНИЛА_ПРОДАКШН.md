# 🚀 Установка на продакшн (drive.technoorganic.info)

**Дата:** 13 марта 2026  
**Исправление:** Конфигурация Apache - директивы внутри VirtualHost

---

## 📦 Быстрая установка (автоматически)

### 1. Скопируйте файлы на сервер

```bash
cd /home/smidt/nc1c
./upload_to_production.sh
```

Или вручную:

```bash
cd /home/smidt
scp install_production.sh root@drive.technoorganic.info:/tmp/
scp INSTALL_PRODUCTION.md root@drive.technoorganic.info:/tmp/
scp one_c_web_client_v3_deploy.tar.gz root@drive.technoorganic.info:/tmp/
```

### 2. Подключитесь к серверу

```bash
ssh root@drive.technoorganic.info
```

### 3. Запустите установку

```bash
cd /tmp
chmod +x install_production.sh
./install_production.sh
```

Скрипт:
- ✅ Скопирует приложение в `/var/www/html/nextcloud/apps/`
- ✅ Настроит Apache (добавит ProxyPass внутри VirtualHost)
- ✅ Включит модули Apache
- ✅ Установит приложение в Nextcloud
- ✅ Перезапустит Apache
- ✅ Создаст резервную копию

---

## 🔧 Ручная установка (если скрипт не работает)

### 1. Распакуйте приложение

```bash
cd /var/www/html/nextcloud/apps
tar -xzf /tmp/one_c_web_client_v3_deploy.tar.gz
mv one_c_web_client_v3_clean one_c_web_client_v3
chown -R www-data:www-data one_c_web_client_v3
```

### 2. Настройте Apache

**Откройте конфиг:**
```bash
nano /etc/apache2/sites-available/nextcloud.conf
```

**Добавьте ПЕРЕД `</VirtualHost>` (для *:443):**

```apache
    # ===================================================================
    # one_c_web_client_v3 - Прокси для 1С
    # ===================================================================

    SSLProxyEngine on
    SSLProxyVerify none
    SSLProxyCheckPeerCN off
    SSLProxyCheckPeerName off

    # Исключения для статических файлов Nextcloud
    ProxyPass /core !
    ProxyPass /apps !
    ProxyPass /dist !
    ProxyPass /js !
    ProxyPass /css !
    ProxyPass /l10n !
    ProxyPass /index.php !
    ProxyPass /loleaflet !
    ProxyPass /browser !
    ProxyPass /hosting !
    ProxyPass /cool !

    # Прокси для 1С сервера: https://10.72.1.5
    ProxyPass /one_c_web_client_v3 https://10.72.1.5/one_c_web_client_v3 retry=0 timeout=60
    ProxyPassReverse /one_c_web_client_v3 https://10.72.1.5/one_c_web_client_v3
    ProxyPassReverseCookiePath / /

    # WebSocket прокси для 1С
    ProxyPass /one_c_web_client_v3/ws wss://10.72.1.5/one_c_web_client_v3/ws retry=0 timeout=60
    ProxyPassReverse /one_c_web_client_v3/ws wss://10.72.1.5/one_c_web_client_v3/ws

    # Разрешение фреймов и CSP
    Header unset X-Frame-Options
    Header always set Content-Security-Policy "frame-ancestors 'self'; frame-src *; connect-src *; script-src 'self' 'unsafe-inline' 'unsafe-eval' *; style-src 'self' 'unsafe-inline' *;"

    # ===================================================================
    # Конец настроек one_c_web_client_v3
    # ===================================================================
```

**Важно:** Директивы должны быть **ВНУТРИ** `<VirtualHost *:443>`!

### 3. Проверьте и перезапустите Apache

```bash
apache2ctl configtest
# Должно быть: Syntax OK

systemctl restart apache2
```

### 4. Установите приложение в Nextcloud

```bash
cd /var/www/html/nextcloud
sudo -u www-data php occ app:install one_c_web_client_v3
sudo -u www-data php occ maintenance:repair
```

---

## ✅ Проверка

### 1. Проверьте приложение

```bash
sudo -u www-data php occ app:list | grep one_c
```

Должно быть:
```
one_c_web_client_v3: 3.2.0
```

### 2. Проверьте Apache

```bash
apache2ctl configtest
systemctl status apache2
```

### 3. Откройте в браузере

**Админ-панель:**
```
https://drive.technoorganic.info/index.php/settings/admin/one_c_web_client_v3
```

**Приложение:**
```
https://drive.technoorganic.info/index.php/apps/one_c_web_client_v3/
```

---

## 🐛 Решение проблем

### Ошибка "Syntax error" в Apache

**Проблема:** Директивы добавлены вне VirtualHost

**Решение:**
```bash
nano /etc/apache2/sites-available/nextcloud.conf
```

Убедитесь, что все директивы `ProxyPass`, `Header`, `SSLProxy*` находятся **МЕЖДУ** `<VirtualHost *:443>` и `</VirtualHost>`.

### Ошибка "Mixed Content"

**Проблема:** 1С недоступна по HTTPS

**Решение:**
1. Проверьте HTTPS на 1С сервере: `curl -k https://10.72.1.5/`
2. Примите самоподписанный сертификат в браузере

### Ошибка "Connection refused"

**Проблема:** 1С сервер недоступен

**Решение:**
```bash
# Проверьте доступность
ping 10.72.1.5
curl -k https://10.72.1.5/one_c_web_client_v3/
```

---

## 📋 Что исправлено в этой версии

### Версия 3.2.0 (13 марта 2026)

**Исправление ошибки из продакшн:**

❌ **БЫЛО (неправильно):**
```apache
</VirtualHost>

# Директивы ВНЕ VirtualHost - ОШИБКА!
SSLProxyEngine on
ProxyPass /one_c_web_client_v3 ...
```

✅ **СТАЛО (правильно):**
```apache
    # Директивы ВНУТРИ VirtualHost - ПРАВИЛЬНО!
    SSLProxyEngine on
    ProxyPass /one_c_web_client_v3 ...
</VirtualHost>
```

**Все директивы должны быть внутри блока `<VirtualHost *:443>`!**

---

## 📞 Если что-то пошло не так

1. **Проверьте логи Apache:**
   ```bash
   tail -f /var/log/apache2/nextcloud/nextcloud_error.log
   ```

2. **Проверьте статус Apache:**
   ```bash
   systemctl status apache2
   ```

3. **Проверьте приложение:**
   ```bash
   sudo -u www-data php occ app:list
   ```

4. **Восстановите резервную копию:**
   ```bash
   cp /tmp/one_c_backup_*/apache_config.backup /etc/apache2/sites-available/nextcloud.conf
   apache2ctl graceful
   ```

---

**Готово!** 🎉
